You are analyzing one archive image. Return a compact JSON object that matches the schema exactly.

Rules:
- Return JSON only.
- Return one compact JSON object on a single line if possible.
- Be brief and literal.
- Prefer short phrases over long prose.
- If something is unclear, leave it null or omit it where allowed.
- OCR should include all readable text. Return null if none is readable.
- `short_summary` must be one plain sentence, 8 to 18 words.
- `search_tags` should contain 5 to 12 short tags when possible.
- `search_tag_details` should contain the same important tags with a short confidence score.
- `scene.description` must be 1 to 2 short sentences.
- `artistic_notes` should comment briefly on composition, lighting, visible detail, and resolution.
- `objects` should include the most useful visible subjects, up to 8.
- For each object, include a short `color` when clearly visible and 0 to 3 short `details` such as material, clothing, brand, pose, or notable feature.
- Avoid duplicate object entries. Merge repeated items into one object with a higher `count`.
- `place_candidates` should include only likely matches.
- Do not identify people by name.

Schema:
{
  "schema_version": "image_v1",
  "ocr_text": string | null,
  "short_summary": string | null,
  "search_tags": [string],
  "search_tag_details": [
    { "label": string, "confidence": float }
  ],
  "scene": {
    "setting": "outdoor" | "indoor" | "mixed" | null,
    "lighting": "daylight" | "golden_hour" | "night" | "artificial" | "mixed" | null,
    "weather": string | null,
    "mood": string | null,
    "primary_subject": string | null,
    "description": string
  },
  "artistic_notes": {
    "summary": string | null,
    "composition": string | null,
    "lighting": string | null,
    "detail": string | null,
    "resolution": string | null
  } | null,
  "objects": [
    { "label": string, "confidence": float, "bbox": { "x": float, "y": float, "w": float, "h": float } | null, "count": int, "color": string | null, "details": [string] }
  ],
  "place_candidates": [
    { "name": string, "country": string | null, "region": string | null, "place_type": string | null, "confidence": float, "reasoning": string | null }
  ],
  "person_regions": [
    { "bbox": { "x": float, "y": float, "w": float, "h": float }, "apparent_age_group": string | null, "gender_presentation": string | null, "is_prominent": bool }
  ],
  "confidence_overall": float,
  "extraction_notes": string | null
}
